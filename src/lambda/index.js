exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Exist VPC with CDK - Hello from Lambda!'),
    };
    return response;
};
